---
mode: agent
description: A reusable prompt template. Invoke it in VS Code chat with /example.
---

# Example Prompt

Replace this with a reusable task prompt. Reference workspace variables such as
`${selection}`, `${file}`, or `${input:name}` as needed.

Describe exactly what you want Copilot to do when this prompt runs.
